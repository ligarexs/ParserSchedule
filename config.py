"""
    Настройки проекта.
    Всё, что может меняться от семестра к семестру, лежит здесь,
    не в коде парсера
"""

# Что парсим
GROUP = "Б-ИТЗД 24/1 Р"          # название группы как в шапке xlsx
COURSE = "3 курс 4 года"          # как назван курс на сайте универа

"""
    Сколько подгрупп у группы. Нужно, чтобы отличить "пара на всю группу
    от "пара только у одной подгруппы", когда в тексте нет маркера вида 1/2
"""
SUBGROUP_COUNT = 2

# Короткие названия предметов
"""
    Ключ — подстрока (в нижнем регистре), которая ищется в полном названии
    из xlsx. Значение — как предмет должен выглядеть в гугл-таблице.
    Порядок важен: проверяется сверху вниз, первое совпадение выигрывает
"""
SUBJECT_SHORT_NAMES = {
    "обработка больших данных": "Big Data",
    "bigdata": "Big Data",
    "криптографические методы": "Криптография",
    "основы микропроцессорной": "Микропроцессоры",
    "защита информации в веб": "Защита веб-прилож.",
    "деловой иностранный язык": "Английский",
    "объектно-ориентированное программирование": "ООП",
    "нейронные сети": "Нейросети",
    "основы искусственного интеллекта": "ИИ",
    "разработка startup": "StartUp",
    "интернет вещей": "IoT",
    "3d-моделирование": "3D-моделирование",
}

# Порядок дней в таблице и минимальная высота блока дня
WEEK_DAYS = ["ПН", "ВТ", "СР", "ЧТ", "ПТ"]
MIN_ROWS_PER_DAY = 5

# Дни, которые парсер не трогает никогда (ведутся вручную в manual_week.json).
# Пусто = парсер управляет всей неделей.
MANUAL_DAYS = []

# Подпись недели в заголовке
WEEK_LABEL = "Неделя IV"

# Ручные уточнения

"""
    Комментарии, для случаев, когда информации из файла недостаточно
"""
SUBJECT_OVERRIDES = {
    "Английский": {
        "room": "308/309",
        "note": "* По английскому, Сапаров ведёт в 308-й, Нураханова в 309-й",
    },
}

# Google Sheets
WORKSHEET_NAME = "Расписание"

try:
    from local_config import SERVICE_ACCOUNT_FILE, SPREADSHEET_ID
except ImportError as exc:
    raise RuntimeError(
        "Не найден local_config.py. Скопируйте local_config.py.example "
        "в local_config.py и впишите SPREADSHEET_ID и путь к ключу."
    ) from exc


# Левый верхний угол блока расписания
ANCHOR_ROW = 2
ANCHOR_COL = 2     # B

# Сколько строк вниз от якоря чистить перед записью (страховка от того,
# что прошлая неделя была длиннее новой)
CLEAR_ROWS = 40
FONT_FAMILY = "Rubik"

# Цвета ячеек
COLOR_LECTURE = (0.85, 0.92, 0.83)      # зелёный — лекции
COLOR_SEMINAR = (1.00, 0.95, 0.80)      # оранжевый — семинары
COLOR_ONLINE = (0.81, 0.87, 0.95)       # синий — онлайн-лекции
COLOR_EMPTY = (1.00, 1.00, 1.00)

# --- Файлы -----------------------------------------------------------------

SCHEDULE_DIR = "schedules"        # скачанные xlsx
DB_PATH = "schedule_state.db"
SCHEDULE_URL = "https://esil.edu.kz/students_schedule/"
REQUEST_TIMEOUT = 15
